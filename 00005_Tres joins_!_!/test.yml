- type: query
  expected: SELECT clientes.nombre, clientes.apellido, productos.nombre, marcas.nombre from clientes left join ventas ON ventas.id_cliente = clientes.id left JOIN productos ON productos.id = ventas.id_producto left join marcas on marcas.id = productos.id_marca;