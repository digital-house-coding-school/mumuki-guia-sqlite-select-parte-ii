- type: query
  expected: SELECT clientes.nombre, clientes.apellido, productos.nombre from clientes inner join ventas ON ventas.id_cliente = clientes.id INNER JOIN productos ON productos.id = ventas.id_producto;